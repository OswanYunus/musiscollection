import ThreeDProduct from '../components/3DProduct';

export default function Home() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      {/* Add your page content here: title, description, call-to-action, etc. */}
      <ThreeDProduct />
    </div>
  );
}